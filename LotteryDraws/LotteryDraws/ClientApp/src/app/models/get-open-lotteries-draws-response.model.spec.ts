import { GetOpenLotteriesDrawsResponse } from './get-open-lotteries-draws-response.model';

describe('GetOpenLotteriesDrawsResponse', () => {
  it('should create an instance', () => {
    expect(new GetOpenLotteriesDrawsResponse()).toBeTruthy();
  });
});
