import { TattsSvcErrorInfo } from './tatts-svc-error-info.model';

describe('TattsSvcErrorInfo', () => {
  it('should create an instance', () => {
    expect(new TattsSvcErrorInfo()).toBeTruthy();
  });
});
