import { OpenLotteriesDraw } from './open-lotteries-draw.model';

describe('OpenLotteriesDraw', () => {
  it('should create an instance', () => {
    expect(new OpenLotteriesDraw()).toBeTruthy();
  });
});
