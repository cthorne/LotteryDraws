import { LotteriesCompany } from './lotteries-company.model';

describe('LotteriesCompany', () => {
  it('should create an instance', () => {
    expect(new LotteriesCompany()).toBeTruthy();
  });
});
